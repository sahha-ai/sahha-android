package sdk.sahha.android.domain.model.enums

import androidx.annotation.Keep

@Keep
enum class SahhaFramework {
    android_kotlin,
    ios_swift,
    react_native,
    flutter,
    capacitor,
    cordova
}
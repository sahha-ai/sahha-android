package app  // Adjust to match your app's package

import android.app.Application

class SahhaApplication : Application() {
    override fun onCreate() {
        super.onCreate()
        // Optional: Initialize SDK or other globals here if needed
    }
}